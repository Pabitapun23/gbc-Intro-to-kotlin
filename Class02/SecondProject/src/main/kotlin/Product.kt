class Product {
    var name:String
    var quantity:Int
    var price:Double

    constructor(productName:String, q:Int, unitPrice:Double) {
        name = productName
        quantity = q
        price = unitPrice
    }
}